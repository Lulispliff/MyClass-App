import 'package:flutter/material.dart';

class ProfessorInitScreen extends StatelessWidget {
  const ProfessorInitScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
